
# **Как удалить все ветви кроме основной?**




## 🔥 **Как удалить все ветки кроме основной**

### Вариант 1: Удалить локальные ветки (в Termux)

```bash
# Переключись на основную ветку
git checkout Основная

# Удали все остальные локальные ветки
git branch | grep -v "Основная" | xargs git branch -D
```

**Что делает:**
- `git branch` — показывает все ветки
- `grep -v "Основная"` — исключает основную ветку
- `xargs git branch -D` — удаляет все остальные

---

### Вариант 2: Удалить ветки на GitHub (удаленные)

```bash
# Посмотреть все ветки на GitHub
git branch -r

# Удалить конкретную ветку с GitHub (например, main)
git push origin --delete main

# Или удалить все, кроме Основной
git push origin --delete $(git ls-remote --heads origin | grep -v "Основная" | awk '{print $2}' | sed 's#refs/heads/##')
```

---

### Вариант 3: Удалить и локальные, и удаленные

```bash
# Переключись на Основную
git checkout Основная

# Удали все локальные ветки
git branch | grep -v "Основная" | xargs git branch -D

# Удали все удаленные ветки (кроме Основной)
git push origin --delete $(git ls-remote --heads origin | grep -v "Основная" | awk '{print $2}' | sed 's#refs/heads/##')
```

---

### Вариант 4: Простой скрипт для Termux

```bash
#!/bin/bash
# Сохрани это как ~/clean_branches.sh

echo "Переключаюсь на Основную..."
git checkout Основная

echo "Удаляю локальные ветки..."
git branch | grep -v "Основная" | xargs git branch -D

echo "Удаляю ветки на GitHub..."
git push origin --delete $(git ls-remote --heads origin | grep -v "Основная" | awk '{print $2}' | sed 's#refs/heads/##') 2>/dev/null

echo "Готово! Осталась только ветка Основная."
```

Запусти:
```bash
chmod +x ~/clean_branches.sh
~/clean_branches.sh
```

---

## ⚠️ **ВАЖНО!**

1. **Сначала убедись, что ты в правильной ветке:**
   ```bash
   git branch
   # Должно показывать * Основная
   ```

2. **Проверь, какие ветки есть перед удалением:**
   ```bash
   git branch -a
   ```

3. **Если хочешь оставить только одну ветку и на телефоне, и на GitHub:**
   ```bash
   git checkout Основная
   git branch | grep -v "Основная" | xargs git branch -D
   git push origin --all --force
   ```

---

**Какая у тебя сейчас основная ветка? `Основная` или `main`?**



